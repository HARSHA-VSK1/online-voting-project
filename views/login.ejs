<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
<body class="bg-gray-50">
    <div class=" flex text-gray-700 flex-col overflow-hidden bg-white-50 sm:py-12">
        <div class="py-3 sm:w-96 mx-auto text-center">
            <span class="text-xl font-bold">Login To Your Account</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-blue-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="mt-5 bg-white opacity-50 hover:opacity-100 shadow-md rounded-lg text-left">
                <form action="/session" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="email" class="block mt-2 font-semibold">Email</label>
                    <input id="email" name="email" type="email" placeholder="Email" class="border w-full h-5 px-3 py-5 mt-5 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <label for="password" class="block mt-5 font-semibold">Password</label>
                    <input id="password" name="password" type="password" placeholder="Password" class="border w-full h-5 px-3 py-5 mt-5 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <button type="submit" class="mt-8 w-full bg-blue-500 text-white py-2 px-6 rounded-md hover:bg-blue-600 ">Login</button>
                </form>
            </div>
            <div class="mt-5">
                <a href="/signup" class="text-sm hover:text-blue-400">Don't have account? Sign up</a>
            </div>
        </div>
    </div>
</body>
</html>