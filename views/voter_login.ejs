<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
<body class="bg-[url('/images/bgind.jpg')] bg-cover">
    <div class=" flex  text-gray-800 antialiased flex-col  overflow-hidden  py-6 sm:py-12">
        <div class=" mt-12 py-3 sm:w-96 mx-auto text-center">
            <span class="font-serif text-2xl font-bold ">Login as Voter</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-blue-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="font-serif mt-4 bg-white shadow-md rounded-lg text-left">
                <form action="/election/<%= url %>/voter/" method="post" class="opacity-90 px-12 py-12">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="VoterID" class="block mt-3 font-semibold">Voter ID</label>
                    <input id="VoterID" name="VoterID" type="text" placeholder="Voter ID" class="border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <label for="Password" class="block mt-3 font-semibold">Password</label>
                    <input id="Password" name="Password" type="password" placeholder="Password" class="border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <button type="submit" class="mt-6 w-full bg-blue-500 text-white py-2 px-6 rounded-md hover:bg-blue-600 ">Login</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>