<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css" />
    <title><%= title %></title>
    <style>
      body{
        background: url("images/indexbac.jpg");
        background-size: cover;
        background-repeat: no-repeat;    
      }
    </style>
  </head>
  <body>
    <nav class="p-9">
      <div class="container flex flex-wrap items-center justify-center mx-auto ">
          <span class="text-red-600 items-center text-4xl font-semibold whitespace-nowrap">ONLINE VOTING PLATFORM</span>
      </div>
    </nav> 
    <div>
        <% if (messages && messages.error && messages.error.length > 0) { %>
            <% for(var i=0; i<messages.error.length; i++) { %>
                <p class="bg-blue-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
            <% } %>
        <% } %>
    </div>
    <br/><br/>
    <div class="flex flex-col items-center text-left overflow-hidden bg-white-50 sm:py-12 ">
      <div class="mt-1 bg-white opacity-50 hover:opacity-100 shadow-md rounded-lg text-left"></div>
        <form class="mt-1 bg-white opacity-50 hover:opacity-100 shadow-md rounded-lg text-left px-8 py-6 bg-blue-50">
          <p class="py-1 text-3xl">
            Welcome To The Platform
          </p>
          <br/>
          <p class="py-1 text-xl">
            First Time? <a class="text-blue-600" href="/signup">Sign Up</a>
          </p>
          <br/>
          <p class="py-1 text-xl">
            Already been Here?
            <a class="text-blue-600" href="/login">Sign In</a>
          </p>
        </form>
    </div>
    </div>
  </body>
</html>
