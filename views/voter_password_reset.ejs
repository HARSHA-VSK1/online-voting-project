<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
</head>
<body class="bg-[url('/images/bgelec.jpg')] bg-cover">
  <nav class="p-6 font-serif bg-white">
    <div class="container flex flex-wrap items-center justify-between mx-auto">
      <span class="flex items-center self-center text-3xl font-semibold whitespace-nowrap text-blue-700">Online Voting Platform</span>
      <div class="w-auto">
        <ul class="flex">
          <li>
            <a href="/user_password_reset" class="block px-4 py-2 font-semibold text-blue-700 rounded hover:bg-red-100">Reset password</a>
          </li>
          <li>
            <a href="/signout" class="block px-4 py-2 font-semibold text-blue-700 rounded hover:bg-red-100">Sign out</a>
          </li>
        </ul>
      </div>
    </div>
</nav>
    <div class="font-serif  text-xl flex gap-1 mx-10 mt-6 ">
        <a href="/elections" class="text-blue-800 hover:text-blue-600">All Elections</a>
        <span>></span>
        <a href="/elections/<%= electionID %>/" class="text-blue-800 hover:text-blue-600">Election</a>
        <span>></span>
        <a href="/elections/<%= electionID %>/voters" class="text-blue-800 hover:text-blue-600">Voters</a>
    </div>
    <div class=" flex  text-gray-800 antialiased flex-col  overflow-hidden py-6 sm:py-12">
        <div class=" py-3 sm:w-96 mx-auto text-center">
            <span class="text-2xl font-bold ">Reset voter password</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-blue-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="mt-4 bg-white shadow-md rounded-lg text-left">
                <form action="/elections/<%= electionID %>/voters/<%= VoterID %>/edit" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="new-password" class="block mt-3 font-semibold">New password</label>
                    <input id="new-password" name="new_password" type="password" placeholder="New password" class="border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <button type="submit" class="mt-6 bg-blue-500 text-white w-full py-2 px-6 rounded-md hover:bg-blue-600 ">Change password</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>